import { Grid } from './styles';
import Header from '../Header';
import Content from '../Content';

const Layout = () => (
  <Grid>
    <Header />
    <Content>
      <h1>Ola</h1>
    </Content>
  </Grid>
);

export default Layout;
